// cypress/integration/app.spec.js

// describe("Navigation", () => {
//   it("should navigate to the search page", () => {
//     // Start from the index page
//     cy.visit("http://localhost:3000/");

//     // Find a link with an href attribute containing "support" and click it
//     cy.get('a[href*="search"]').click();

//     // The new url should include "/support"
//     cy.url().should("include", "/support");

//     // // The new page should contain an h1 with "About page"
//     // cy.get("h1").contains("About Page");

//     //The new page should contain a div with <SupportPageComponent />
//     cy.get("h1").contains("Hello");
//   });
// });
